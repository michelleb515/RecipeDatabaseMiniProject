/**
 * 
 */
$(document).ready(function() {
		
	//Disable recipe buttons.  They will be re-enabled when a radio button is selected. 
	$('.rcpbtn').prop('disabled',true);
	
	//var cal = document.getElementById('cal'); 
	//cal.style.color="red";
	//alert("cal = " + cal.val()); 

});
